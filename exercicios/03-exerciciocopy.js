


// function verificarSePodeSerAdotado(idade, porte) {
//   // Permite a adoção se a idade for 1 e o porte for 'M'
//   if (idade === 1 && porte === 'M') {
//     return true;
//   }
//   // Permite a adoção se a idade for diferente de 1 e o porte for diferente de 'M'
//   if (idade !== 1 && porte !== 'M') {
//     return true;
//   }
//   // Caso contrário, não permite
//   return false;
// }

function verificarSePodeSerAdotado(idade, porte) {
   // Permite a adoção apenas se a idade for 1 e o porte for 'M'n 
   return idade === 1 && porte === 'M';
    }
    console.log(verificarSePodeSerAdotado)
// / Assim, somente quando idade for 1 e porte for 'M', 